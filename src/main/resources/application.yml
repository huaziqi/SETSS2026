spring:
  application: name:SETSS2026
  datasource:
    url: jdbc:mysql://47.109.38.44:3306/setss2026?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC&characterEncoding=utf8
    username: lhz
    password: 123456
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update # 自动更新表结构（开发环境）
    show-sql: true # 显示SQL语句
    properties:
      hibernate:
        format_sql: true # 格式化SQL输出
